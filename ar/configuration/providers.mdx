---
title: "اختيار مزوّد"
description: "ضبط مزودي نماذج اللغة: DeepSeek وXiaomi MiMo وKimi وMiniMax وAnthropic وOpenAI وOllama"
---

# المزودون

يتواصل Wolffish مع نماذج اللغة الكبيرة عبر سبعة مزودين سحابيين بالإضافة لخيار محلي، جميعها باستخدام `fetch()` خالص — بدون أي SDK. لكل مزود تنسيق تدفق خاص به واتفاقية استدعاء أدوات خاصة به، يوحدها `wernicke.ts` في واجهة واحدة.

## اختيار المزوّد

جميع المزودين السحابيين يتعاملون مع المهام الوكيلية — بما في ذلك سلاسل الأدوات المعقدة متعددة الخطوات. الفرق هو التكلفة مقابل السقف.

**الطبقة الاقتصادية — DeepSeek وMiMo وKimi وMiniMax** تتعامل مع المهام الوكيلية المعقدة بشكل جيد — سلاسل أدوات طويلة متعددة الخطوات، وسير عمل البحث، وتوليد الأكواد، والأتمتة المستقلة. يجب أن تكون خيارك الافتراضي. بتكلفة أقل بـ 5–25 ضعفًا من الطبقة المتميزة. ابدأ هنا وارتقِ فقط إذا وجدت أن التنفيذ غير موثوق بما يكفي لسير عمل محدد.

**الطبقة المتميزة — Anthropic وOpenAI** تقدمان أقوى قدرة نموذجية. يتفوق Claude Opus 4.7 وGPT-5.5 حيث تقصر الطبقة الاقتصادية — خاصة **computer-use** (التفاعل مع الشاشة)، الذي يدعمه Anthropic فقط، والحالات الحرجة حيث لا يكون التنفيذ على النماذج الأرخص كافيًا.

| الطبقة | المزوّد | النموذج الرئيسي | إدخال / إخراج (لكل مليون رمز) | الأفضل لـ |
|--------|---------|----------------|-------------------------------|----------|
| اقتصادي | **DeepSeek** | deepseek-v4-pro | $0.435 / $0.87 | الافتراضي لمعظم المهام الوكيلية |
| اقتصادي | **MiMo** | mimo-v2.5-pro | $0.20 / $2.00 | الأرخص، متعدد اللغات |
| اقتصادي | **Kimi** | kimi-k2.6 | $0.95 / $4.00 | استدلال قوي، سياق طويل |
| اقتصادي | **MiniMax** | MiniMax-M3 | $0.30 / $1.20 | استدلال وبرمجة |
| متميز | **Anthropic** | claude-opus-4.7 | $5.00 / $25.00 | المهام الأصعب، computer-use |
| متميز | **OpenAI** | gpt-5.5 | $5.00 / $30.00 | المهام الأصعب، معرفة واسعة |
| محلي | **Ollama** | متنوع | مجاني | الخصوصية، احتياطي دون اتصال |

### متى تنتقل للطبقة المتميزة

- **Computer-use / التفاعل مع الشاشة** — يدعمها Anthropic فقط؛ لا بديل
- **التنفيذ غير موثوق بما يكفي** — إذا جربت مهمة على DeepSeek أو MiMo والوكيل يفشل باستمرار، انتقل إلى Anthropic أو OpenAI لذلك السير المحدد

### توصيتنا

ابدأ بـ DeepSeek أو MiMo. يتعاملان مع المهام الوكيلية المعقدة — سلاسل أدوات طويلة، وسير عمل بحث، وتوليد أكواد، وأتمتة مستقلة — بجزء من التكلفة. جرّب سير عملك الفعلي. إذا لم ينفذ سير عمل محدد بموثوقية، انتقل إلى Anthropic أو OpenAI لتلك المهمة. معظم المستخدمين يجدون أنهم نادرًا ما يحتاجون لذلك.

<Tip>
  اضبط DeepSeek أو MiMo كمزوّدك الأساسي وضع Anthropic أو OpenAI كاحتياطي. تدرج Wolffish يتعامل مع هذا تلقائيًا — تنفيذ اقتصادي افتراضيًا مع شبكة أمان متميزة.
</Tip>

## DeepSeek (موصى به)

```
POST https://api.deepseek.com/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI.

**DeepSeek V4 Pro هو الخيار الموصى به افتراضيًا للمهام الوكيلية في Wolffish.** بعد [تخفيض السعر الدائم بنسبة 75%](https://deepseek.ai/blog/deepseek-v4-pro-api-price-cut-permanent) (مايو 2026)، يقدم استدلالًا بمستوى حدودي بتكلفة أقل بـ 29–34 ضعفًا من النماذج الحدودية المنافسة على أحمال الإخراج — مع مطابقة أو تجاوز أدائها الوكيلي. كما أنه مرخّص بـ MIT، فيمكنك استضافته ذاتيًا بتكلفة $0 إن كانت لديك البنية التحتية.

الأفضل لـ: سير العمل الوكيلي متعدد الخطوات، استدعاء الأدوات، سلاسل البحث، الأتمتة اليومية الفعّالة من حيث التكلفة.

<Tip>
  إذا كنت تُعدّ Wolffish لأول مرة وتريد مزوّدًا واحدًا يفعل كل شيء — استدعاء أدوات موثوق، استدلال قوي، ردود سريعة، تكلفة ضئيلة — ابدأ بـ DeepSeek V4 Pro. يمكنك دائمًا إضافة Anthropic أو OpenAI لاحقًا لحالات استخدام محددة.
</Tip>

### الحصول على مفتاح API

1. اذهب إلى [platform.deepseek.com](https://platform.deepseek.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← DeepSeek

### النماذج

| النموذج | السياق | الإخراج الأقصى | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|----------------|-------------------------------|---------|
| **deepseek-v4-pro** | 1M | 384K | $0.435 / $0.87 | الموصى به افتراضيًا. أداء وكيلي حدودي. تخزين مؤقت: $0.003625/م.رمز. |
| deepseek-v4-flash | 1M | 384K | $0.14 / $0.28 | سريع ورخيص. تخزين مؤقت: $0.0028/م.رمز. |

---

## Xiaomi MiMo

```
POST https://api.xiaomimimo.com/v1/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI. يدعم الرؤية (صور base64) ومستندات PDF.

بعد [تخفيض Xiaomi الدائم للأسعار بنسبة تصل إلى 99%](https://www.studioglobal.ai/ms/discover/answers/what-prompted-xiaomi-to-cut-its-mimo-v2-5-6a179fe3f9dbfce068be8b7e) — لتطابق أسعار DeepSeek V4 Pro — يعد MiMo V2.5 من أرخص المزودين السحابيين المتاحين.

الأفضل لـ: سير العمل الوكيلي الاقتصادي، المهام متعددة اللغات، سلاسل أدوات متعددة الخطوات، الأتمتة عالية الحجم.

### الحصول على مفتاح API

1. اذهب إلى [platform.xiaomimimo.com](https://platform.xiaomimimo.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← Xiaomi Mimo

### النماذج

| النموذج | السياق | الإخراج الأقصى | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|----------------|-------------------------------|---------|
| **mimo-v2.5-pro** | 1M | 64K | $0.20 / $2.00 | أفضل نموذج MiMo. استدلال قوي ومتعدد اللغات. |
| mimo-v2.5 | 1M | 32K | $0.08 / $0.80 | توازن جيد بين الجودة والتكلفة. |
| mimo-v2-flash | 256K | 16K | $0.01 / $0.30 | رخيص للغاية للمهام عالية الحجم. |

---

## Kimi (Moonshot AI)

```
POST https://api.moonshot.ai/v1/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI. يدعم الرؤية ومحتوى الاستدلال.

الأفضل لـ: سير العمل الوكيلي، المهام ذات السياق الطويل، أحمال العمل المكثفة بالاستدلال.

### الحصول على مفتاح API

1. اذهب إلى [platform.moonshot.ai](https://platform.moonshot.ai)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← Kimi

### النماذج

| النموذج | السياق | الإخراج الأقصى | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|----------------|-------------------------------|---------|
| **kimi-k2.6** | 256K | 64K | $0.95 / $4.00 | الأحدث. استدلال قوي. |
| kimi-k2.5 | 256K | 64K | $0.60 / $3.00 | توازن جيد بين التكلفة والجودة. |
| moonshot-v1-128k | 128K | 16K | $2.00 / $5.00 | سياق طويل. |

---

## MiniMax

```
POST https://api.minimaxi.chat/v1/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI. يدعم محتوى الاستدلال.

الأفضل لـ: أحمال العمل المكثفة بالاستدلال، توليد الأكواد، سير العمل الوكيلي. يبقى DeepSeek وMiMo أرخص وأكثر قدرة لمعظم أحمال العمل.

### الحصول على مفتاح API

1. اذهب إلى [platform.minimaxi.chat](https://platform.minimaxi.chat)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← MiniMax

### النماذج

| النموذج | السياق | الإخراج الأقصى | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|----------------|-------------------------------|---------|
| **MiniMax-M3** | 1M | 64K | $0.30 / $1.20 | الأحدث. استدلال قوي. |
| MiniMax-M2.7 | 200K | 32K | $0.30 / $1.20 | الجيل السابق. |
| MiniMax-M2.5 | 200K | 32K | $0.30 / $1.20 | توازن بين الجودة والتكلفة. |

---

## Anthropic (Claude)

```
POST https://api.anthropic.com/v1/messages
```

يستخدم تدفق SSE. تصل استدعاءات الأدوات ككتل محتوى `tool_use`.

الأفضل لـ: الاستدلال المعقد، واتباع التعليمات التفصيلية، والاستخدام الدقيق للأدوات، وcomputer-use (التفاعل مع الشاشة).

### الحصول على مفتاح API

1. اذهب إلى [console.anthropic.com](https://console.anthropic.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← Anthropic

### النماذج

| النموذج | السياق | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|-------------------------------|---------|
| claude-opus-4.7 | 200K | $5.00 / $25.00 | الأحدث. الأقوى في الاستدلال والكتابة. |
| **claude-sonnet-4** | 200K | $3.00 / $15.00 | أفضل توازن بين الجودة والتكلفة. |
| claude-haiku-4 | 200K | $0.80 / $4.00 | سريع ورخيص. غير موصى به للمهام الوكيلية. |

<Note>
  Anthropic هو المزوّد الوحيد الذي يدعم **computer-use** (التفاعل مع الشاشة). إن احتجت أن يقود Wolffish متصفحًا أو واجهة سطح مكتب، تحتاج مفتاح Anthropic.
</Note>

---

## OpenAI (GPT)

```
POST https://api.openai.com/v1/chat/completions
```

يستخدم تدفق SSE. تصل استدعاءات الأدوات ككائنات `function_call`.

الأفضل لـ: المهام العامة، والمعرفة الواسعة، والردود السريعة.

### الحصول على مفتاح API

1. اذهب إلى [platform.openai.com](https://platform.openai.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← OpenAI

### النماذج

| النموذج | السياق | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|-------------------------------|---------|
| gpt-5.5 | 200K | $5.00 / $30.00 | الأحدث. استدلال قوي. |
| **gpt-4o** | 128K | $2.50 / $10.00 | نموذج OpenAI الموصى به للمهام الوكيلية. |
| gpt-4o-mini | 128K | $0.15 / $0.60 | سريع ورخيص. موثوقية أدوات محدودة. |
| o3 | 200K | $10.00 / $40.00 | استدلال موسّع. تكلفة عالية. |
| o4-mini | 200K | $1.10 / $4.40 | استدلال بتكلفة أقل. |

---

## Ollama (محلي)

```
POST http://localhost:11434/api/chat
```

يستخدم تدفق NDJSON. تصل استدعاءات الأدوات كـ JSON مُهيكَل في الاستجابة. لا حاجة لمفتاح API — يعمل بالكامل على جهازك. راجع [دليل تكامل Ollama](/ar/configuration/ollama) لمتطلبات النماذج وتوصيات العتاد.

الأفضل لـ: الخصوصية، والاستخدام دون اتصال، والتجريب بلا تكلفة، والبديل المتوفر دائمًا.

## تتبع الصحة

يتتبع `thalamus` صحة كل مزود بشكل مستقل:

- **عدد الإخفاقات** — يزيد عند كل طلب فاشل
- **فترة التهدئة** — تراجع أسّي بعد الإخفاقات
- **فحص الاتصال** — `net.isOnline()` للكشف الفوري عن وضع عدم الاتصال

عندما يفشل مزود، يدخل فترة تهدئة قبل أن تُعاد محاولته. يتخطى التدرج المزودين غير الأصحاء وينتقل مباشرة إلى التالي المتاح.

## اختيار مزود أساسي

اضبط مزودك الأساسي في الإعدادات أو في `config.json`. يتدرج بهذا الترتيب: **DeepSeek → MiMo → Kimi → MiniMax → Claude → OpenAI → Ollama** (يتخطى غير المُعدّين). تُجرَّب مزودك الأساسي أولًا، ثم يتولى التدرج عند الفشل.

جميع المزودين السبعة السحابيين وOllama اختياريون — تحتاج واحدًا مُعدًّا على الأقل. إن استخدمت مزودي السحابة فقط، يتخطى التدرج Ollama. إن استخدمت Ollama فقط، يكون هو المزوّد الوحيد.

<Tip>
  للحصول على أفضل تجربة، أبقِ Ollama يعمل مع نموذج مُنزَّل كشبكة أمان. حتى لو كنت تستخدم Claude في المقام الأول، فإن وجود بديل محلي يعني أنك لن تعلق أبدًا بلا رد عند انقطاع الاتصال.
</Tip>
