---
title: "OpenAI"
description: "إعداد OpenAI (GPT) — المهام العامة والمعرفة الواسعة والردود السريعة"
---

# OpenAI (GPT)

```
POST https://api.openai.com/v1/chat/completions
```

يستخدم تدفق SSE. تصل استدعاءات الأدوات ككائنات `function_call`.

الأفضل لـ: المهام العامة، والمعرفة الواسعة، والردود السريعة.

## الحصول على مفتاح API

1. اذهب إلى [platform.openai.com](https://platform.openai.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في Wolffish ← الإعدادات ← النماذج ← OpenAI

## النماذج

| النموذج | السياق | الإخراج الأقصى | الأوضاع | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|----------------|---------|-------------------------------|---------|
| gpt-5.5-pro | 1M | 64K | بدون، عالي، أقصى | $30.00 / $180.00 | الأقوى. استدلال حدودي. |
| **gpt-5.5** | 1M | 64K | بدون، عالي، أقصى | $5.00 / $30.00 | الرائد. استدلال قوي. تخزين مؤقت: $0.50/م.رمز. |
| gpt-5.4 | 1M | 64K | بدون، عالي، أقصى | $2.50 / $15.00 | الجيل السابق. تخزين مؤقت: $0.25/م.رمز. |
| gpt-5.4-mini | 1M | 64K | بدون، عالي، أقصى | $0.75 / $4.50 | استدلال سريع. تخزين مؤقت: $0.08/م.رمز. |
| gpt-5.4-nano | 1M | 64K | بدون، عالي، أقصى | $0.20 / $1.25 | استدلال فائق الرخص. تخزين مؤقت: $0.02/م.رمز. |

<Note>
  **الأوضاع** تتحكم في عمق الاستدلال عبر `reasoning_effort`. *بدون* يعطّل الاستدلال للردود السريعة. *عالي* يُفعّل الاستدلال القياسي. *أقصى* يُفعّل الاستدلال العميق. جميع النماذج تدعم استدعاء الأدوات بدون حد أقصى لعددها.
</Note>
