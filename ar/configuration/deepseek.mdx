---
title: "DeepSeek"
description: "إعداد DeepSeek — المزوّد الموصى به افتراضيًا للمهام الوكيلية في وولفيش"
---

# DeepSeek (موصى به)

```
POST https://api.deepseek.com/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI.

**DeepSeek هو المزوّد الموصى به افتراضيًا للمهام الوكيلية في وولفيش.** تقدّم عائلة V4 استدلالًا بمستوى حدودي بجزء يسير مما تتقاضاه النماذج الحدودية المنافسة على أحمال الإخراج — مع مطابقة أو تجاوز أدائها الوكيلي. كما أنه مرخّص بـ MIT، فيمكنك استضافته ذاتيًا بتكلفة $0 إن كانت لديك البنية التحتية. ومنذ أغسطس 2026 يعتمد DeepSeek فوترة **الذروة/خارج الذروة** (الذروة 01:00–04:00 و06:00–10:00 بتوقيت UTC؛ وخارج الذروة بنصف السعر) — ولهذا تُقرأ الأسعار أدناه كنطاقات.

يختار وصلُ DeepSeek نموذج **V4 Flash** تلقائيًا — سريعٌ، وبثلث سعر Pro، ومبنيٌّ للأدوار المثقلة بالأدوات التي يتكوّن منها أكثر العمل الوكيلي. أمّا **V4 Pro** فهو النموذج الحدودي في الثنائي، على بعد اختيارٍ واحد من مبدّل النماذج للمهامّ الاستدلالية الأثقل.

الأفضل لـ: سير العمل الوكيلي متعدد الخطوات، استدعاء الأدوات، سلاسل البحث، الأتمتة اليومية الفعّالة من حيث التكلفة.

<Tip>
  إذا كنت تُعدّ وولفيش لأول مرة وتريد مزوّدًا واحدًا يفعل كل شيء — استدعاء أدوات موثوق، استدلال قوي، ردود سريعة، تكلفة ضئيلة — ابدأ بـ DeepSeek واترك الافتراضي V4 Flash كما هو. يمكنك دائمًا إضافة Anthropic أو OpenAI لاحقًا لحالات استخدام محددة.
</Tip>

## الحصول على مفتاح API

1. اذهب إلى [platform.deepseek.com](https://platform.deepseek.com)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **API Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في وولفيش ← الإعدادات ← النماذج ← DeepSeek

## النماذج

| النموذج | السياق | الأوضاع | إدخال / إخراج (لكل مليون رمز) | ملاحظات |
|---------|--------|---------|-------------------------------|---------|
| **deepseek-v4-pro** | 1M | إيقاف، عالٍ، أقصى | $0.66–1.32 / $1.98–3.96 | النموذج الحدودي في الثنائي — الطبقة الاستدلالية الأثقل. تخزين مؤقت: $0.022–0.044/م.رمز. |
| deepseek-v4-flash | 1M | إيقاف، عالٍ، أقصى | $0.22–0.44 / $0.66–1.32 | يُختار تلقائيًا عند الوصل. سريع، أداء وكيلي قوي، رخيص. تخزين مؤقت: $0.007–0.014/م.رمز. |
| deepseek-v4-flash-vision-exp | 1M | إيقاف، عالٍ، أقصى | $0.22–0.44 / $0.66–1.32 | نموذج Flash مع رؤية تجريبية. تخزين مؤقت: $0.007–0.014/م.رمز. |

## الرؤية

كان DeepSeek نصّيّاً بحتاً عبر تشكيلته كلّها حتى **21 أغسطس 2026**، حين وصل **deepseek-v4-flash-vision-exp** — نموذجُ V4 Flash وقد فُتحت عيناه، بسعر Flash وسرعته. اختره ويصير الوكيل ينظر حقّاً: لقطات الشاشة والصور المرفقة واقتصاصات [`image_view`](/ar/capabilities/built-in-capabilities) تبلغ النموذجَ بكسلاتٍ، متحقَّقاً منها حيّاً على الواجهة نفسها. أمّا نماذج V4 النصّيّة المجرّدة فتبقى بلا بصر — جزءُ الصورة يُرفَض رفضاً — فيعمل الوكيل عليها من الشروح والأدوات لا من الرؤية، وبوّابةُ الرؤية في وولفيش تضمن ألّا تُرسَل البكسلات إلّا حيث تُقبَل. ولاحقة `-exp` صدقُ DeepSeek نفسِه: النموذج تجريبيّ، فتوقَّع أن يتحرّك.

## أوضاع الاستدلال

تتحكّم أيقونة **الدماغ** بجوار صندوق الرسائل في طريقة استدلال النموذج. اضغط عليها للتنقّل بين الأوضاع التي يدعمها النموذج المحدّد. تجتمع هنا فكرتان منفصلتان:

### التفكير — *هل* يفكّر النموذج

- **إيقاف** — يجيب النموذج فورًا. الأسرع والأرخص؛ مثاليّ للمهام البسيطة والمباشرة.
- **تشغيل** — يعالج النموذج المسألة أولًا في مرحلة استدلال مخصّصة قبل الردّ. أبطأ ويستهلك رموزًا أكثر، لكنه أدقّ بوضوح في المهام متعدّدة الخطوات أو المنطقية أو الغامضة.

### الجهد — *إلى أي مدى* يفكّر

تُتيح هذا النماذج التي تدعم الجهد فقط، ويُطبَّق عندما يكون التفكير مُفعّلًا.

- **عالٍ** — عمق استدلال قياسي. الخيار الافتراضي المناسب لمعظم المهام.
- **أقصى** — يفكّر النموذج لفترة أطول وأعمق لأصعب المسائل. رموز وكمون أكثر مقابل جودة أعلى في الأعمال المعقّدة.

### حالات الزر

| الحالة | اللون | المعنى |
|-------|------|-------|
| إيقاف | رمادي | التفكير متوقّف — إجابة مباشرة |
| تشغيل | أزرق | التفكير مُفعّل — دون تحكّم بالجهد |
| عالٍ | بنفسجي | التفكير مُفعّل، جهد قياسي |
| أقصى | برتقالي | التفكير مُفعّل، أقصى جهد |

يعرض كل نموذج الحالات التي يدعمها فعليًا فقط. إذا كان النموذج يفكّر دائمًا (لا يمكن إيقافه) أو لا يملك تحكّمًا بالجهد، فإن الزر يعكس ذلك ويُقفَل حيث لا يوجد ما يتغيّر. يحفظ Wolffish اختيارك لكل نموذج.

**في ديب-سيك:** تدعم نماذج V4 كلها الأوضاع إيقاف / عالٍ / أقصى — ومنذ **الإصدار 1.0.251** صارت الإعدادات الثلاثة مختلفة حقًا. كان حقل الجهد يُرسَل في الطلب على مستوى أعمق مما ينبغي بدرجة واحدة، حيث لا شيء يشتكي ولا شيء يُصغي، فقاس «عالٍ» و«أقصى» فارقًا أقل من ثلث بالمئة — إعدادان لم يُرسَلا قط. ومع الحقل في موضعه الصحيح وبقياس حيّ: **«إيقاف» لا يفكّر إطلاقًا، و«عالٍ» يفكّر بجدّ، و«أقصى» يفكّر قرابة ضعف مدة «عالٍ»** في مسألة تكافئ ذلك.

---

## اختيار النموذج وإعادة المحاولة

يتواصل وولفيش مع نماذج اللغة الكبيرة عبر تسعة مزودين سحابيين بالإضافة لخيار محلي، جميعها باستخدام `fetch()` خالص — بدون أي SDK. لكل مزود تنسيق تدفق خاص به واتفاقية استدعاء أدوات خاصة به، يوحدها `wernicke.ts` في واجهة واحدة.

اختر نموذج دماغك صراحةً من **مبدّل النماذج بجوار حقل الإدخال** — النموذج الذي تختاره هو من يعمل. لا يوجد تدرّج ولا ترتيب احتياطي؛ وللعمل المتوازي، انقل المحادثة إلى [وضع سير العمل](/ar/configuration/workflow-mode)، حيث يستطيع القائد تشغيل وكلاء على أيّ نموذج موصول، وديب-سيك من بينها.

عندما يصطدم دماغ السحابة بخطأ عابر، يُعيد `thalamus` محاولة النموذج **نفسه** وفق جدول تراجع (كما يستخدم `net.isOnline()` للكشف الفوري عن وضع عدم الاتصال). ولا يوجّهك إلى مزوّد مختلف عند الفشل.
