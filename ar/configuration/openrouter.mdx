---
title: "OpenRouter"
description: "إعداد OpenRouter — مُجمّع نماذج للوصول إلى أي مزوّد عبر مفتاح API واحد"
---

# OpenRouter (مُجمّع)

```
POST https://openrouter.ai/api/v1/chat/completions
```

يستخدم تدفق SSE بتنسيق أدوات متوافق مع OpenAI. يوجّه الطلبات إلى أي نموذج من أي مزوّد عبر مفتاح API واحد.

**OpenRouter هو مُجمّع نماذج** — نقطة نهاية API واحدة تُوكّل الطلبات إلى Anthropic وOpenAI وDeepSeek وQwen وxAI وMeta وMistral وGoogle وعشرات غيرهم. مفتاح واحد، فاتورة واحدة، وصول لكل شيء.

الأفضل لـ: تجربة نماذج لم تُعدّها مباشرة، فاتورة موحّدة، الوصول لنماذج غير مدعومة أصليًا في وولفيش.

<Warning>
  **نوصي بضبط المزودين مباشرة كلما أمكن.** التكامل المباشر يمنحك زمن استجابة أقل (بدون قفزة وسيطة)، وتتبع تكاليف دقيق، وميزات خاصة بالمزوّد (التخزين المؤقت العابر في Anthropic، وFIM في DeepSeek)، وبدون هامش وسيط. يضيف OpenRouter طبقة توجيه قد تزيد زمن الاستجابة وتسبب سلوكًا غير متسق أحيانًا.

  استخدم OpenRouter عندما تريد تجربة نماذج لم تُعدّها مباشرة، أو كبديل مناسب لمزودين لا تريد إدارة مفتاح API منفصل لهم.
</Warning>

## الحصول على مفتاح API

1. اذهب إلى [openrouter.ai](https://openrouter.ai)
2. سجّل حسابًا أو سجّل الدخول
3. انتقل إلى **Keys** وأنشئ مفتاحًا جديدًا
4. الصقه في وولفيش ← الإعدادات ← النماذج ← OpenRouter

## النماذج

يوجّه OpenRouter إلى مئات النماذج عبر عشرات المزوّدين. الجدول أدناه قائمة مختارة بأشهر 20 نموذجًا — ويعرض المنتقي نماذج أكثر بكثير بمجرد توصيل مفتاحك:

| النموذج | السياق | الأوضاع | إدخال / إخراج (لكل مليون رمز) | مُخزَّن مؤقتًا |
|---------|--------|---------|-------------------------------|----------------|
| **anthropic/claude-opus-4.1** | 200K | إيقاف، عالٍ | $15.00 / $75.00 | — |
| anthropic/claude-sonnet-4.5 | 1M | إيقاف، عالٍ | $3.00 / $15.00 | — |
| openai/gpt-5 | 400K | إيقاف، عالٍ | $1.25 / $10.00 | — |
| openai/gpt-5-mini | 400K | إيقاف، عالٍ | $0.25 / $2.00 | — |
| openai/o3 | 200K | إيقاف، عالٍ | $2.00 / $8.00 | — |
| openai/o4-mini | 200K | إيقاف، عالٍ | $1.10 / $4.40 | — |
| openai/gpt-4o | 128K | — | $2.50 / $10.00 | — |
| openai/gpt-4.1 | 1M | — | $2.00 / $8.00 | — |
| google/gemini-2.5-pro | 1M | إيقاف، عالٍ | $1.25 / $10.00 | — |
| google/gemini-2.5-flash | 1M | إيقاف، عالٍ | $0.30 / $2.50 | — |
| deepseek/deepseek-r1 | 164K | إيقاف، عالٍ | $0.70 / $2.50 | — |
| deepseek/deepseek-chat-v3.1 | 164K | إيقاف، عالٍ | $0.21 / $0.79 | — |
| x-ai/grok-4.3 | 1M | إيقاف، عالٍ | $1.25 / $2.50 | — |
| qwen/qwen3-235b-a22b | 131K | إيقاف، عالٍ | $0.45 / $1.82 | — |
| z-ai/glm-4.6 | 203K | إيقاف، عالٍ | $0.43 / $1.74 | — |
| meta-llama/llama-4-maverick | 1M | — | $0.15 / $0.60 | — |
| meta-llama/llama-3.3-70b-instruct | 131K | — | $0.10 / $0.32 | — |
| mistralai/mistral-large | 128K | — | $2.00 / $6.00 | — |
| mistralai/mistral-medium-3 | 131K | — | $0.40 / $2.00 | — |
| moonshotai/kimi-k3 | 1M | إيقاف، عالٍ | $3.00 / $15.00 | — |

<Note>
  هذه قائمة مختارة مختصرة وليست الكتالوج الكامل. يتيح OpenRouter مئات النماذج عبر مفتاح واحد — ويعرض المنتقي نماذج أكثر بكثير من الشائعة المعروضة هنا. لا يتتبّع وولفيش خصومات التخزين المؤقت لكل رمز للنماذج الموجَّهة، لذا فإن عمود التخزين المؤقت غير متاح في جميع الحالات.
</Note>

## التكامل المباشر مقابل OpenRouter

| | مزوّد مباشر | OpenRouter |
|--|-------------|-----------|
| **زمن الاستجابة** | الأقل — استدعاء API مباشر | أعلى — قفزة وسيطة إضافية |
| **التكلفة** | أسعار المزوّد فقط | أسعار المزوّد + هامش OpenRouter |
| **الميزات** | ميزات المزوّد الكاملة | مجموعة فرعية مُوحّدة |
| **الفوترة** | منفصلة لكل مزوّد | فاتورة موحّدة واحدة |
| **الوصول للنماذج** | المزودون المُعدّون فقط | مئات النماذج بمفتاح واحد |
| **التخزين المؤقت** | أصلي للمزوّد (Anthropic العابر، DeepSeek، إلخ) | يختلف حسب المزوّد الأساسي |

### متى تستخدم OpenRouter

**مناسب:**
- تجربة نماذج من مزودين لم تُعدّهم بعد
- اختبار A/B سريع بين عائلات نماذج مختلفة
- فاتورة موحّدة عندما تريد فاتورة API واحدة فقط
- الوصول لنماذج متخصصة أو أحدث غير مدعومة أصليًا بعد

**استخدم التكامل المباشر بدلاً من ذلك عندما:**
- المزوّد مدعوم أصليًا بالفعل (DeepSeek، Anthropic، OpenAI، إلخ)
- تحتاج أقل زمن استجابة ممكن
- تريد ميزات خاصة بالمزوّد (تخزين مؤقت، بادئات الموجّهات، إلخ)
- تُشغّل أحمال عمل إنتاجية عالية الحجم حيث تتراكم القفزة الوسيطة

<Tip>
  إذا كنت تستخدم بالفعل DeepSeek أو Anthropic أو أي مزوّد مدعوم أصليًا، أبقِ على الاتصال المباشر. أضف OpenRouter فقط لنماذج لا يمكنك الوصول إليها مباشرة — ثم اختر نموذج OpenRouter كدماغك حين تريد استخدامه. لا يوجد تدرّج؛ النموذج الذي تختاره هو من يعمل.
</Tip>

## أوضاع الاستدلال

تُضبَط طريقة استدلال النموذج من **بطاقة النموذج بجوار حقل الإدخال** — مرِّر المؤشّر فوق مبدّل النماذج لمعاينة البطاقة، واضغط لتثبيتها مفتوحة. وصفُّ الشرائح العلويّ، المعنون **التفكير**، هو أداة التحكّم: اختر شريحة فتسري ابتداءً من رسالتك التالية. تجتمع هنا فكرتان منفصلتان:

### التفكير — *هل* يفكّر النموذج

- **إيقاف** — يجيب النموذج فورًا. الأسرع والأرخص؛ مثاليّ للمهام البسيطة والمباشرة.
- **تشغيل** (تُقرأ الشريحة **عادي**) — يعالج النموذج المسألة أولًا في مرحلة استدلال مخصّصة قبل الردّ. أبطأ ويستهلك رموزًا أكثر، لكنه أدقّ بوضوح في المهام متعدّدة الخطوات أو المنطقية أو الغامضة.

### الجهد — *إلى أي مدى* يفكّر

تُتيح هذا النماذج التي تدعم الجهد فقط، ويُطبَّق عندما يكون التفكير مُفعّلًا.

- **عالٍ** — عمق استدلال قياسي. الخيار الافتراضي المناسب لمعظم المهام.
- **أقصى** — يفكّر النموذج لفترة أطول وأعمق لأصعب المسائل. رموز وكمون أكثر مقابل جودة أعلى في الأعمال المعقّدة.

### صفّ الشرائح

| الشريحة | الأيقونة | المعنى |
|--------|---------|-------|
| معطل | برق | التفكير متوقّف — إجابة مباشرة |
| عادي | دماغ | التفكير مُفعّل — دون تحكّم بالجهد |
| عالي | دماغ ذكيّ | التفكير مُفعّل، جهد قياسي |
| أقصى | لهب | التفكير مُفعّل، أقصى جهد |

الشريحة الفاعلة مُلوّنة، وما عداها هادئ. ويعرض كل نموذج الشرائح التي يدعمها فعليًا فقط — فالنموذج الذي يفكّر دائمًا لا يملك **معطل**، والنموذج ذو الوضع الواحد يعرض شريحته وحدها غير قابلة للضغط، والنموذج الذي لا يستدلّ إطلاقًا يستبدل بالصفّ عبارة *«هذا النموذج لا يدعم الاستدلال.»* ويحفظ وولفيش اختيارك لكل نموذج. (قبل **الإصدار 1.0.236** كان هذا زرَّ دماغٍ ملوَّنًا على شريط الكتابة؛ ثم انتقل إلى بطاقة النموذج، بجوار صفّ فردي/سير العمل، لتجتمع كلُّ مقابض النموذج في لوحة واحدة.)

**في OpenRouter:** يعتمد الاستدلال على النموذج الموجَّه. تعرض النماذج القادرة على الاستدلال إيقاف / عالٍ؛ ولا تملك النماذج غير الاستدلالية تحكّمًا. يحدّ OpenRouter الجهد عند «عالٍ»، وبعض النقاط (مثل GPT-5 وDeepSeek-R) تفكّر إجباريًا — وفيها يتحوّل «إيقاف» إلى حدّ أدنى من الاستدلال.

## استخدام OpenRouter كنموذجك

لا يوجد تدرّج مزودين ولا ترتيب أولوية. لاستخدام نموذج OpenRouter، اختره كدماغك من **مبدّل النماذج بجوار حقل الإدخال** (أو دع القائد يختاره لأحد وكلائه في [وضع سير العمل](/ar/configuration/workflow-mode)). النموذج الذي تختاره هو من يعمل — وليس OpenRouter احتياطيًا تلقائيًا خلف مزوديك الآخرين.
